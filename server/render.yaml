services:
  - type: web
    name: kineticpeak-server
    runtime: node
    plan: free
    rootDir: kineticpeak/server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
